# RunPod
runpod==1.7.13

# 🔥 DIFFUSERS (WAN REQUIRED)
diffusers @ git+https://github.com/huggingface/diffusers.git@main

# HuggingFace stack
transformers>=4.38.0
accelerate>=0.27.0
huggingface_hub>=0.20.3
safetensors>=0.4.2

# Core numeric / image
numpy==1.26.4
Pillow>=10.0.0

# Torch (NO instalar torch aquí, ya viene en la imagen CUDA)
# torch viene del nvidia/cuda base image
